import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://gnuboss.github.io',
  // Remove the base line below if you rename the repo to: gnuboss.github.io
  // base: '/your-repo-name',
});
